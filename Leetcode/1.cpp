class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_set<int> u;
        for(int i : nums){
            if(u.count(target - i)){
                return 
            }
        }
    }
};
